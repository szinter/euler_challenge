const { __task__(camelCase)Runner } = require('./runner');
const { __task__(camelCase) } = require('./__task__(camelCase)');

module.exports = {
    __task__(camelCase),
    run: __task__(camelCase)Runner
}